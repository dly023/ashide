# Scripts

Small repo-local utilities not wired into CI yet.

| Script | Purpose |
|--------|---------|
| [`check_i18n_orphans.sh`](check_i18n_orphans.sh) | List orphan / possibly-dynamic Fluent keys in `app/i18n/en/warp.ftl` |

Release and platform bundling live under [`../script/`](../script/) (e.g. `script/bundle`, `script/make_release_artifacts`).
