# Scripts

Small repo-local utilities used by repository checks.

| Script | Purpose |
|--------|---------|
| [`check_i18n_orphans.sh`](check_i18n_orphans.sh) | Fail closed when an English Fluent key has no literal macro reference or explicitly modeled dynamic owner |

Release and platform bundling live under [`../script/`](../script/) (e.g. `script/bundle`, `script/make_release_artifacts`).
